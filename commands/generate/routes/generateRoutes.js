const { createDirectory } = require("../../../utils");
const { createFile } = require("../../../utils");

const generateRoutes = (userInput) => {
  console.log("Tried to generate routes");
};

module.exports = { generateRoutes };
